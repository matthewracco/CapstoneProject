VITE_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here

# For local dev: point directly at the local backend
# In production (Vercel), leave this unset so the app uses the
# same-origin /api/v1 rewrite defined in vercel.json.
VITE_API_URL=http://localhost:5050/api/v1
